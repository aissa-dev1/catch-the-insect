import Game from "../classes/Game";
import Score from "../classes/Score";
import Timer from "../classes/Timer";

type InsectHandlerOptions = {
  game: Game;
  timer: Timer;
  score: Score;
};

export default InsectHandlerOptions;
